<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crrcdt.meeting.mapper.UserMapper">
    <select id="getUserById" resultType="com.crrcdt.meeting.model.User">
        select * from user where id = #{id};
    </select>
</mapper>